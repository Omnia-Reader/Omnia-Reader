/**
 * Remote providers remain available to their focused integration tests, but
 * are not part of the shipped local-reader experience until authentication
 * and credential management have a supported deployment path.
 */
export const REMOTE_SYNC_ENABLED = false;
