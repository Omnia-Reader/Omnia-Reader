/**
 * Remote synchronization is available in production and full-stack
 * development builds. Local web development replaces this module with the
 * local-only variant so a missing gateway cannot trigger provider traffic.
 */
export const REMOTE_SYNC_ENABLED = true;
